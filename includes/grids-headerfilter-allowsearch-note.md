[note]With the **ODataStore**, searching a numeric column requires additional configuration: set the column's **headerFilter**.[searchMode]({basewidgetpath}/Configuration/columns/headerFilter/#searchMode) to *"equals"* and specify the type of the column's data field in the store's [fieldTypes](/Documentation/ApiReference/Data_Layer/ODataStore/Configuration/#fieldTypes) option.
